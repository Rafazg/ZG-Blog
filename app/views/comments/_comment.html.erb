<%= stylesheet_link_tag('layout/show_layout') %>

<div class="comment-<%= comment.id %> container"
    style="border: 1px solid black; padding: 1em; margin: 1em; height: 200px;">

    <%= comment.user.name%><br />
    <span>Posterd <%= time_ago_in_words(comment.created_at) %></span>
    <% if current_user == comment.user %>
        <div class="button-group float-end">
            <%= button_to "Delete", [post, comment], class:"btn btn-danger", method: :delete %>
        </div>

    <% end %>
    <hr />
    <%= comment.body %>
</div>